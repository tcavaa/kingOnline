# Dependencies
node_modules/

# Env / secrets
.env
.env.local
.env.production
.env.*.local

# Build output — client/dist is COMMITTED on purpose: shared hosting can't
# run the Vite build, so the deploy ships the locally-built bundle.
build/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.log
npm-debug.log*

# Old SQLite artifacts (in case they come back via better-sqlite3)
server/data/
*.sqlite
*.sqlite-shm
*.sqlite-wal
